# CA Office Suite — Real App (Django)

This folder is the real web application (login + dashboard + masters).

## Local setup (later)
- Create a Python venv
- `pip install -r requirements.txt`
- Run migrations and start server

We’ll keep development on SQLite by default and switch to PostgreSQL in DigitalOcean.

